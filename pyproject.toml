[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "compressez-pm-gmod"
version = "1.2.0"
description = "Compresseur d'addons Playermodel pour Garry's Mod (GUI + CLI)"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "TenGoKu13" }]
keywords = ["garrysmod", "gmod", "playermodel", "gma", "vtf", "compression"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Environment :: Win32 (MS Windows)",
    "Environment :: X11 Applications",
    "Intended Audience :: End Users/Desktop",
    "Natural Language :: French",
    "Natural Language :: English",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Topic :: Games/Entertainment",
    "Topic :: Multimedia :: Graphics :: Graphics Conversion",
]
dependencies = []

[project.optional-dependencies]
gui = ["tkinterdnd2>=0.5.0"]
images = ["Pillow>=10.0.0"]
vtf = ["srctools>=2.7.0"]
dev = ["pytest>=7.0.0"]

[project.urls]
Homepage = "https://github.com/TenGoKu13/Compressez-PM-gmod"
Issues = "https://github.com/TenGoKu13/Compressez-PM-gmod/issues"
Changelog = "https://github.com/TenGoKu13/Compressez-PM-gmod/blob/main/CHANGELOG.md"

[project.scripts]
compressez-pm = "cpm.cli:cli_main"

[tool.setuptools]
packages = ["cpm"]

[tool.pytest.ini_options]
testpaths = ["tests"]
