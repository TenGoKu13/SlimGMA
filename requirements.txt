# Requis pour optimiser les textures .png / .jpg / .tga
Pillow>=10.0.0

# Glisser-déposer dans l'interface graphique (inclus automatiquement dans l'.exe)
tkinterdnd2>=0.5.0

# Note : les .vtf sont compressés nativement (troncature de mipmaps),
# aucune dépendance supplémentaire n'est nécessaire.
